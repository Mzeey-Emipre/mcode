/** @internal Temporary compatibility path for server-local Providers during staged cutover. */
export {
  SessionRuntime,
  type ProtocolAdapter,
  type SpawnArgs,
  type SpawnResult,
} from "../private/session-runtime.js";
